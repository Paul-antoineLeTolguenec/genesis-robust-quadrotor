# CLAUDE.md

All project rules, architecture, constraints, and workflows live in `AGENTS.md` and `.agents/`.

**On session start, read (in order):**
1. `ROADMAP.md` — current milestone
2. `AGENTS.md` — project overview, principles, commands, skills
3. `.agents/knowledge/architecture.md` — module graph and data flow
4. `.agents/knowledge/constraints.md` — numbered hard rules

Load Tier 2 topic docs from `.agents/knowledge/topics/` on demand, per the routing table in `.agents/knowledge/README.md`.
